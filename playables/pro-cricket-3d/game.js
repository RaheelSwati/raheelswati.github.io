const game=document.querySelector('#game'),canvas=document.querySelector('#pitch'),ctx=canvas.getContext('2d'),intro=document.querySelector('#intro'),result=document.querySelector('#result'),callout=document.querySelector('#callout'),score=document.querySelector('#score'),scoreNum=score.querySelector('b');let active=false,ball=null,startY=0;
function size(){canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)}addEventListener('resize',size);size();
function begin(){intro.style.display='none';result.classList.remove('show');canvas.style.display='block';callout.style.display='block';score.style.display='block';scoreNum.textContent='0';active=true;ball={x:innerWidth*.5,y:innerHeight*.25,t:0};requestAnimationFrame(loop)}
function loop(){if(!active)return;ball.t+=.012;ball.y=innerHeight*(.25+ball.t*.54);ball.x=innerWidth*.5+Math.sin(ball.t*5)*8;ctx.clearRect(0,0,innerWidth,innerHeight);ctx.strokeStyle='#fff8';ctx.lineWidth=3;ctx.setLineDash([8,9]);ctx.beginPath();ctx.moveTo(innerWidth*.5,innerHeight*.25);ctx.lineTo(innerWidth*.5,innerHeight*.82);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='#e3342f';ctx.beginPath();ctx.arc(ball.x,ball.y,8,0,7);ctx.fill();if(ball.t>1.05)finish(0);else requestAnimationFrame(loop)}
function hit(){if(!active)return;const timing=Math.abs(ball.t-.78);finish(timing<.1?6:timing<.22?4:1)}
function finish(runs){active=false;callout.style.display='none';scoreNum.textContent=runs;const v=document.querySelector('#verdict'),t=document.querySelector('#resultTitle');v.textContent=runs===6?'SIX!':runs===4?'FOUR!':runs===1?'1 RUN':'BOWLED!';t.textContent=runs>=4?'PERFECT TIMING':runs?'GOOD CONTACT':'TRY THE NEXT BALL';setTimeout(()=>result.classList.add('show'),350)}
game.addEventListener('pointerdown',e=>startY=e.clientY);game.addEventListener('pointerup',e=>{if(startY-e.clientY>25||active)hit()});document.querySelector('#start').onclick=begin;document.querySelector('#again').onclick=begin;
